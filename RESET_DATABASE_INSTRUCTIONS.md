# 🔄 تعليمات إعادة تعيين قاعدة البيانات

## المشكلة
إذا كان التطبيق يستخدم البيانات القديمة ولم يتم تحديثها إلى قاعدة البيانات الجديدة.

---

## ✅ الحل السريع

### الطريقة 1: إعادة تثبيت التطبيق
1. احذف التطبيق من الجهاز/المحاكي
2. أعد تشغيل التطبيق من Android Studio
3. ستُنشأ قاعدة البيانات من جديد

### الطريقة 2: مسح بيانات التطبيق
**على المحاكي/الجهاز:**
1. اذهب إلى الإعدادات (Settings)
2. التطبيقات (Apps)
3. ابحث عن تطبيقك
4. اضغط على "مسح البيانات" (Clear Data)
5. أعد تشغيل التطبيق

### الطريقة 3: استخدام ADB
```bash
adb shell pm clear com.example.h1
```

---

## 🔧 الحل البرمجي (مؤقت)

تم تعديل `MainActivity.java` لإعادة تعيين قاعدة البيانات تلقائياً:

```java
// في MainActivity.java - السطر الحالي:
DataSeeder.seedDatabase(this, true); // true = إعادة تعيين
```

### ⚠️ مهم جداً:
**بعد أول تشغيل ناجح، غيّر السطر إلى:**
```java
DataSeeder.seedDatabase(this, false); // false = لا تعيد التعيين
```

أو ببساطة:
```java
DataSeeder.seedDatabase(this);
```

---

## 📝 التحقق من نجاح التحديث

بعد إعادة تشغيل التطبيق:

### 1. تسجيل الدخول كأدمن:
- البريد: `admin@nursery.com`
- كلمة المرور: `admin123`

### 2. تحقق من الحضانات:
- يجب أن ترى 5 حضانات
- أول حضانتين بمعلومات حقيقية (مسقط، السيب)

### 3. تحقق من التقييمات:
- من لوحة الأدمن → التعليقات والتقييمات
- يجب أن ترى تقييمات بالعربية

---

## 🎯 الخطوات الكاملة

### الخطوة 1: إعادة تشغيل التطبيق
```
1. افتح Android Studio
2. اضغط Shift + F10 (أو زر Run)
3. انتظر حتى يُثبت التطبيق
```

### الخطوة 2: التحقق
```
1. افتح التطبيق
2. اضغط "ابدأ الآن"
3. يجب أن ترى الحضانات بالعربية
```

### الخطوة 3: تسجيل دخول الأدمن
```
1. من القائمة → الأدمن
2. البريد: admin@nursery.com
3. كلمة المرور: admin123
4. يجب أن تدخل بنجاح
```

### الخطوة 4: إزالة إعادة التعيين التلقائي
```java
// في MainActivity.java
// غيّر من:
DataSeeder.seedDatabase(this, true);

// إلى:
DataSeeder.seedDatabase(this);
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا تزال البيانات القديمة
**الحل:**
```bash
# من Terminal في Android Studio
adb shell
cd /data/data/com.example.h1/databases/
rm nursery_app.db
exit
# ثم أعد تشغيل التطبيق
```

### المشكلة: خطأ عند تسجيل الدخول
**الحل:**
- تأكد من استخدام البريد الإلكتروني: `admin@nursery.com`
- وليس اسم المستخدم: `admin`

### المشكلة: الحضانات فارغة
**الحل:**
```java
// تأكد من أن DataSeeder.seedDatabase() يُستدعى في onCreate
```

---

## ✅ التأكد من نجاح التحديث

### علامات النجاح:
- ✅ الحضانات تظهر بأسماء عربية
- ✅ تسجيل دخول الأدمن يعمل بالبريد الإلكتروني
- ✅ التقييمات بالعربية
- ✅ يمكن للأدمن حذف الحضانات
- ✅ الحجوزات تُحفظ في قاعدة البيانات

---

## 📞 إذا استمرت المشكلة

1. **احذف التطبيق تماماً** من الجهاز/المحاكي
2. **نظف المشروع:**
   ```
   Build → Clean Project
   Build → Rebuild Project
   ```
3. **أعد تشغيل التطبيق**
4. **تحقق من Logcat** للأخطاء

---

## 🎉 بعد النجاح

لا تنسَ تغيير السطر في `MainActivity.java`:
```java
// من:
DataSeeder.seedDatabase(this, true);

// إلى:
DataSeeder.seedDatabase(this);
```

هذا يمنع إعادة تعيين قاعدة البيانات في كل مرة تفتح التطبيق.

